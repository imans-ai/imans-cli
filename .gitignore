.DS_Store
dist/
# The built binary at the repo root only. Anchored with a leading slash so it
# does not also match the cmd/imans/ source directory.
/imans
coverage.out
/opencode.json

# Local agent skill installs created by `npx skills add`.
/.agents/
/skills-lock.json
